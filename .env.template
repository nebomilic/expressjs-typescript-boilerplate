# Add all of the environmental variables here instead of 
# embedding them directly in the app and utilize them 
# with the `DotEnv` package.

PORT=3001
DB_ENDPOINT=<your local database url>
DB_PORT=<your local database port>
DB_USER=<your local database username>
DB_PASS=<your local database password>
DB_NAME=<your local database name>